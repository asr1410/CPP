#include <iostream>
#include <bits/stdc++.h>
#include <cmath>
#include <fstream>
#include <string>
#include <cstdio>
#include <algorithm>
using namespace std;
//STL --> STANDARD TEMPLATE LIBRARY
//STL = container[object which store data]
// + algorithm[procedure of store data]
// + itrators[object which point to an element of a container]
//* CONTAINER
//->sequence container --> linear fashion -> vector, list,  dequeue
//->associative container --> direct access -> tree, set, multiset, map, multimap.
//->derived container ->real world modeling ->stack, queue, pririty queue
int main()
{

    return 0;
}